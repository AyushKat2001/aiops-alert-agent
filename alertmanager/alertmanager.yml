global:
  resolve_timeout: 5m 

route:
  group_by: ['alertname',"severity"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'aiops-agent'
 
receivers:
  - name: 'aiops-agent'
    webhook_configs:
      - url: 'http://agent:5001/alerts'
        send_resolved: true